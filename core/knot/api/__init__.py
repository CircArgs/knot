"""knot HTTP surface — split routers per concern.

`spec` router  — ontology-graph authoring (drafts → publish → published reads).
`model` router — bindings, impl identity, configs, runs (future, post-this-phase).
"""
