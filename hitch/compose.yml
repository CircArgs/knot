name: hitch

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: hitch
      POSTGRES_PASSWORD: hitch
      POSTGRES_DB: hitch
    ports:
      - "5434:5432"
    volumes:
      - hitch_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hitch -d hitch"]
      interval: 5s
      timeout: 5s
      retries: 20

  temporal:
    image: temporalio/auto-setup:1.24
    environment:
      DB: postgres12
      DB_PORT: 5432
      POSTGRES_USER: hitch
      POSTGRES_PWD: hitch
      POSTGRES_SEEDS: postgres
      POSTGRES_DB: temporal
      DBNAME: temporal
      VISIBILITY_DBNAME: temporal_visibility
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "7233:7233"
    healthcheck:
      # Temporal binds to the container hostname; the gRPC server won't
      # send bytes without a request, so just opening + closing the TCP
      # connection is enough to prove the listener is up.
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/$$(hostname)/7233 && exec 3<&-'"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 30s

  temporal-ui:
    image: temporalio/ui:2.27.2
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CORS_ORIGINS: http://localhost:3000
    ports:
      - "8088:8080"
    depends_on:
      - temporal

  hitch-api:
    build:
      context: ..
      dockerfile: hitch/docker/Dockerfile
    command: ["uvicorn", "hitch.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
    environment:
      HITCH_PG_DSN: postgresql://hitch:hitch@postgres:5432/hitch
      HITCH_PG_SCHEMA: hitch
      HITCH_TEMPORAL_ADDRESS: temporal:7233
      HITCH_API_PORT: "8000"
    volumes:
      - ..:/app/repo
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy

  hitch-worker:
    build:
      context: ..
      dockerfile: hitch/docker/Dockerfile
    command: ["python", "-m", "hitch.worker"]
    environment:
      HITCH_PG_DSN: postgresql://hitch:hitch@postgres:5432/hitch
      HITCH_PG_SCHEMA: hitch
      HITCH_TEMPORAL_ADDRESS: temporal:7233
    volumes:
      - ..:/app/repo
    depends_on:
      postgres:
        condition: service_healthy
      temporal:
        condition: service_healthy

volumes:
  hitch_pg:
